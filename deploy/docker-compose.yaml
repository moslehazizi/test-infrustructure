services:
    control-panel-service:
        image: challenge-control-panel-service:TAG_NAME
        container_name: challenge-control-panel-service
        restart: unless-stopped
        ports:
            - "8082:8080"
        env_file:
            - ../.env
        networks:
            - backend

networks:
    backend:
        external: true
